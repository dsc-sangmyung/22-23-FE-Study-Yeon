import React from 'react';
import CommentList from './CommentList';

const PostListItem = () => {
    return (
        <div>
            postlistitem
            <CommentList/>
        </div>
    );
};

export default PostListItem;